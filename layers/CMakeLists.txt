SET(LAYERS testlayer1 testlayer2)

FOREACH(LAYER ${LAYERS})
	FILE(GLOB layer_SRCS ${LAYER}/*.cc)

	INCLUDE_DIRECTORIES(../libgaia)
	ADD_LIBRARY(${LAYER} SHARED ${layer_SRCS})
ENDFOREACH(LAYER ${LAYERS})
